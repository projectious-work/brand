import * as React from 'react';

/** This component takes no props. */
export interface ConvictionsProps {}

/** Numbered conviction list, one rule per row. */
export declare function Convictions(props?: ConvictionsProps): React.ReactElement;
