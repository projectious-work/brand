import * as React from 'react';

/** This component takes no props. */
export interface CTAProps {}

/** Closing call to action on a midnight panel. */
export declare function CTA(props?: CTAProps): React.ReactElement;
