import * as React from 'react';

/** This component takes no props. */
export interface FooterProps {}

/** Site footer — brand mark left, page metadata right. */
export declare function Footer(props?: FooterProps): React.ReactElement;
