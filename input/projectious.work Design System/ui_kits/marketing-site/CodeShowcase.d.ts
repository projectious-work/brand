import * as React from 'react';

/** This component takes no props. */
export interface CodeShowcaseProps {}

/** Two-column section pairing prose with a dark terminal panel. */
export declare function CodeShowcase(props?: CodeShowcaseProps): React.ReactElement;
